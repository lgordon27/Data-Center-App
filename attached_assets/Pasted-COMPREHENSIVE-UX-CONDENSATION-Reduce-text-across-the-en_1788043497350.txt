COMPREHENSIVE UX CONDENSATION: Reduce text across the entire app by approximately 40%. Move secondary content behind expandable details, replace paragraphs with visuals, and make the financial model the unmistakable center of the experience. The principle: if it can be shown, don't write it. If it must be written, one sentence, not three.

SCREEN 1 (Case Brief):
- Keep the Stargate project card and thesis block (Catalyst/Tension/Adjacent/Underwrite). Those are strong.
- Convert the four signal cards into a single compact visual risk matrix: a 2x2 or horizontal strip showing the four signals with icon and one-line label each, not four separate cards with paragraphs.
- Collapse "Public-Site Context" and "Transmission Corridor" into expandable sections. Default state: closed. Show one-line summary with expand arrow.
- The SRI narrative opening ("We Helped Build This") should be 3 sentences maximum, not 4 paragraphs. Keep: "Sustainable investors helped capitalize the AI revolution. Now the infrastructure AI runs on is testing every principle those investors hold. This tool shows what happens when you check the evidence."

SCREEN 2 (Evidence Room):
- Group the 16 evidence items into 4 collapsible category headers: Power & Grid (items 1,3,6,8), Water & Climate (items 2,4,12,13,15), Community & Permitting (items 5,10,14,16), Financial & Counterparty (items 7,9,11). Each category shows a mini summary: "3 of 4 verified" or "2 missing" next to the header.
- Default view for each evidence item: Variable name, current value, classification dropdown. That's it.
- Move description, citation, and source detail into an expandable detail row that opens when the user clicks/taps the item. Collapse by default.
- Remove the introductory framing paragraph entirely. The provenance count cards at the top already communicate the state.
- Keep the one-line SRI framing ("Sustainability ratings grade companies on their disclosures. This evidence room tests whether those disclosures hold up at the site level.") but move it to a subtle subtitle under the screen title, not a standalone paragraph.

SCREEN 3 (Financial Materiality) - THIS IS THE MOST IMPORTANT SCREEN TO FIX:
- The five return metric cards (IRR, MOIC, Cash-on-Cash, Payback, NPV) should be LARGER and more visually dominant. They should feel like a Bloomberg terminal readout, not a card grid.
- Add a sticky sub-navigation or visual cue: "Summary | Drivers | Full Model" so the user knows there are three layers and can jump between them.
- The evidence-to-return bridge should be a VISUAL WATERFALL CHART, not a text table. Show the base case IRR, then each evidence variable's impact as a step up or down, arriving at the current IRR. This is the single most important visualization in the entire app. A waterfall chart communicates "evidence quality changes returns" faster than any table or paragraph.
- Remove the model mechanics explanation paragraph. Replace it with a compact visual flow: Evidence → Timing → Revenue/OPEX → Equity Cash Flow → Return. Five connected nodes, no paragraph.
- The detailed cash-flow table should remain but be in a collapsible "Full Model Detail" section, defaulting to closed. Power users can expand it. Most users see the summary and waterfall.
- Add a prominent visual showing the gap between "All Verified" baseline return and current evidence-adjusted return. This should be unmissable: two large numbers with an arrow and delta between them.

SCREEN 4 (Decision Review):
- This screen is already relatively clean. Keep it.
- Make the "Resolve" pills next to missing evidence items actually link back to the relevant evidence item on Screen 2, so the user can click to go fix the classification. That turns them from status labels into navigation actions.
- Remove any text blocks that repeat information already shown on Screen 3.

SCREEN 5 (Advisor Lens):
- Cut word count from 900-1100 to under 500.
- Remove the three paragraphs before the exposure chain. Replace with one sentence: "Your clients' values-aligned portfolios are directly connected to this infrastructure." Then show the chain.
- Remove the "What the chain means" explanation paragraph after the chain. The chain IS the explanation.
- Replace the "Why This Is Your Role" two paragraphs with a two-column visual comparison:
  Left column: "Traditional Screening" - Reads ratings / Screens funds / Rebalances portfolios / Reports scores
  Right column: "Evidence-Governed Advising" - Tests assumptions / Traces to infrastructure / Interprets for clients / Documents judgment
- Keep the three client-conversation cards but condense each to: Client question (bold), one-sentence framing, one action item. Remove the multi-paragraph explanations within each card.
- Keep the governance gap number. Make it larger and more visually prominent.
- The four Gallup/Edward Jones stat cards are good. Keep those as-is.

HOW IT WORKS:
- Cut from 1400 words to under 700.
- Hero section: reduce from four paragraphs to one. "Traditional sustainability ratings grade what companies reported last year. This tool tests whether the assumptions behind the largest infrastructure investment in history are actually verified. That is the difference between a rating and diligence."
- Screen walkthrough: keep the five cards but remove "Why it matters" from each. Keep only the title, the plain-English summary, and "What to look for." Three fields per card, not four.
- Evidence classification system: keep as-is. The analogies are strong and compact.
- Sources section: collapse all source groups by default. Show group name and count only. Expandable.
- Builder story: reduce to three lines. "Built by LeAndrew Gordon. Former Private Wealth Financial Advisor. Chartered SRI Counselor. Top 25 AI Builder, Replit." Then one sentence: "Sustainability professionals helped build the AI economy. This tool exists because that responsibility extends to the infrastructure layer." Remove the longer biographical paragraph.

VALUE CHAIN:
- Make the chain cards use progressive disclosure. Default: show title and one-line description only. Players and "Why evidence matters" appear on click/tap. This makes the chain scannable at a glance and explorable on interest.
- Remove the "How to use this lens" panel. The chain is self-explanatory.
- Keep the four headline statistics and the pause-signal panel.

HOME PAGE:
- Home page is fine. Minimal changes. Remove any text that duplicates what's on other pages.

GLOBAL RULES:
- No screen should require more than 1.5 viewport scrolls on desktop to reach the bottom.
- Every paragraph should be a candidate for replacement with a visual, a number, or a one-liner.
- If removing text means the user misses context, move it behind an expandable detail, not into the main flow.
- The financial model waterfall chart on Screen 3 should be the visual centerpiece of the entire application. It should be what people screenshot and share on LinkedIn.